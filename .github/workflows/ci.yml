name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    name: Test (Go ${{ matrix.go }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        go: ["1.23"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go }}

      - name: Download dependencies
        run: go mod download

      - name: Run tests with race detector
        run: go test -race -count=1 ./...

  coverage:
    name: Coverage
    runs-on: ubuntu-latest
    needs: test

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.23"

      - name: Download dependencies
        run: go mod download

      - name: Generate test fixtures
        run: |
          pip install pyarrow --quiet
          python testdata/generate.py

      - name: Run tests with coverage
        run: go test -coverprofile=coverage.out -covermode=atomic ./internal/...

      - name: Coverage summary
        run: |
          go tool cover -func=coverage.out | tee coverage.txt
          TOTAL=$(grep total coverage.txt | awk '{print $3}')
          {
            echo "## Coverage Report"
            echo '```'
            cat coverage.txt
            echo '```'
            echo ""
            echo "**Total coverage: ${TOTAL}**"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage.out

  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          # gofmt is a standalone binary and does not follow the toolchain
          # switch that go.mod's `go 1.25.0` triggers for go commands, so this
          # job pins the version the module requires.
          go-version: "1.25"

      - name: Format check
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "gofmt -w . is needed for:"
            echo "$unformatted"
            exit 1
          fi

      - name: Vet
        run: go vet ./...

      - name: Build check
        run: go build ./...
